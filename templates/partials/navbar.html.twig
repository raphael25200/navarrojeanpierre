<nav class="navbar">
  <div class="nav-container">
    <div class="nav-wrapper">
      <a class="brand" href="{{ path('home') }}">JP Navarro</a>

      <button class="burger" id="burger">
        <span></span>
        <span></span>
        <span></span>
      </button>

      <ul class="nav-menu" id="nav-menu">
        <li><a href="{{ path('oeuvres.search') }}">Ses oeuvres</a></li>

        <!-- Catégories affichées directement -->
        <li>
          <span class="categories-title">Catégories</span>
          <ul class="categories-list">
            {% for category in getCategories() %}
              <li><a href="{{ path('oeuvres.category_view', {'slug': category.slug}) }}">{{ category.name }}</a></li>
            {% endfor %}
          </ul>
        </li>

        <li><a href="{{ path('about') }}">Le peintre</a></li>
        <li><a href="{{ path('contact') }}">Contact</a></li>
      </ul>
    </div>
  </div>
</nav>
